{
  "name": "m8-web-display",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:assets": "node scripts/build-assets.js",
    "deploy": "npm run build:assets && npm run build"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "sass": "^1.69.0"
  },
  "dependencies": {
    "juice": "^7.0.0"
  }
}
